<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script>
        let roomId = "<%= roomId %>"
    </script>
    <script src="https://unpkg.com/peerjs@1.4.7/dist/peerjs.min.js" defer></script>
    <script src="/socket.io/socket.io.js" defer></script>
    <script src="/static/join.js" defer></script>
    <link rel="stylesheet" href="/static/bootstrap/bootstrap.min.css" defer>
    <style>
        * {
            margin: 0;
            padding: 0;
            --color-main:rgb(48, 48, 48);
            --color-secondary:rgb(61 61 61);
            color:whitesmoke;
        }

        body {
            background-color: var(--color-main);
        }

        .card-body{
            background-color: var(--color-secondary);
        }

        .form-control{
            background:var(--color-main);
            color:whitesmoke;
        }

        .form-control:focus{
            background:var(--color-main);
            color:whitesmoke;
        }

        .btn-login,.btn-login:active,.btn-login:hover{
            background-color:var(--color-main);
            color:whitesmoke;
            border:1px solid whitesmoke;
        }

        .video-container {
            position: absolute;
            top: 0;
            bottom: 0;
            width: 100%;
            height: 100%;
            overflow: hidden;
            background: var(--color-main);
        }

        .video-container video {
            /* Make video to at least 100% wide and tall */
            min-width: 100%;
            min-height: 100%;

            /* Setting width & height to auto prevents the browser from stretching or squishing the video */
            width: auto;
            height: auto;

            /* Center the video */
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
        }
        .login{
            background:var(--color-secondary)
        }

        .alert-primary {
            background-color: var(--color-main);
            border:1px solid whitesmoke;
            font-weight: bold;
            color:whitesmoke;
            border-color: rgba(255, 0, 0, 0.8);
            box-shadow: 0 1px 1px rgba(0, 0, 0, 0.075) inset, 0 0 8px rgba(255, 0, 0, 0.6);
            outline: 0 none;
        }

        textarea:focus,
        input[type="text"]:focus,
        input[type="password"]:focus,
        input[type="datetime"]:focus,
        input[type="datetime-local"]:focus,
        input[type="date"]:focus,
        input[type="month"]:focus,
        input[type="time"]:focus,
        input[type="week"]:focus,
        input[type="number"]:focus,
        input[type="email"]:focus,
        input[type="url"]:focus,
        input[type="search"]:focus,
        input[type="tel"]:focus,
        input[type="color"]:focus {   
            border-color: rgba(255, 255, 255, 0.8);
            box-shadow: 0 1px 1px rgba(0, 0, 0, 0.075) inset, 0 0 8px rgba(40, 216, 255, 0.6);
            outline: 0 none;
        }
    </style>
</head>

<body>
    <div class="container login-container">
        <div class="row">
          <div class="col-sm-9 col-md-7 col-lg-5 mx-auto">
            <div class="card border-0 shadow rounded-3 my-5">
              <div class="card-body p-4 p-sm-5">
                <h5 class="card-title text-center mb-5 fw-light fs-5">Insert room password</h5>
                <form>
                  <div class="form-floating mb-3">
                    <input type="password" class="form-control" id="pw" placeholder="Password" required>
                    <label for="pw">Password</label>
                  </div>    
                  <div class="d-grid">
                    <button class="btn pt-2 pb-2 btn-login text-uppercase fw-bold" type="submit">Submit</button>
                  </div>              
                </form>
                <div class="mt-3 alert alert-primary" style="display:none;">Text byl zkopírován</div>
              </div>
            </div>
          </div>
        </div>
      </div>
    <div style="display:none;" class="video-container">

    </div>
</body>

</html>